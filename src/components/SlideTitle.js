import React from "react";

const SlideTitle = ({ title }) => {
  return (
    <div>
      <span className="text-[#463198] text-2xl font-semibold">{title}</span>
    </div>
  );
};

export default SlideTitle;
